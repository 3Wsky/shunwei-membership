<view wx:if="{{product}}" class="page">
  <swiper wx:if="{{gallery.length}}" class="banner" circular indicator-dots>
    <swiper-item wx:for="{{gallery}}" wx:key="*this">
      <image class="banner-img" src="{{item}}" mode="aspectFill" />
    </swiper-item>
  </swiper>
  <view class="info">
    <view class="name">{{title}}</view>
    <view class="price">{{priceLabel}}</view>
    <view wx:if="{{subtitle}}" class="desc">{{subtitle}}</view>
  </view>
  <view wx:if="{{colorItems.length}}" class="section">
    <view class="section-title">颜色款式</view>
    <scroll-view class="color-scroll" scroll-x enhanced show-scrollbar="{{false}}">
      <view class="color-row">
        <view wx:for="{{colorItems}}" wx:key="name" class="color-card">
          <image wx:if="{{item.image}}" class="color-img" src="{{item.image}}" mode="aspectFit" />
          <view wx:else class="color-img placeholder">颜色</view>
          <view class="color-name">{{item.name}}</view>
        </view>
      </view>
    </scroll-view>
  </view>
  <view wx:if="{{skuRows.length}}" class="section">
    <view class="section-title">配置价格</view>
    <view wx:for="{{skuRows}}" wx:key="version" class="sku-row">
      <view class="sku-main">
        <view class="sku-version">{{item.version}}</view>
        <view wx:if="{{item.config || item.color}}" class="sku-sub">{{item.config}}{{item.config && item.color ? ' · ' : ''}}{{item.color}}</view>
      </view>
      <view class="sku-price">{{item.price}}</view>
    </view>
  </view>
  <view wx:if="{{paramList.length}}" class="section">
    <view class="section-title">参数规格</view>
    <view wx:for="{{paramList}}" wx:key="name" class="row">
      <text class="label">{{item.name}}</text>
      <text class="value">{{item.value}}</text>
    </view>
  </view>
  <view wx:if="{{specEntries.length}}" class="section">
    <view class="section-title">配置信息</view>
    <view wx:for="{{specEntries}}" wx:key="key" class="row">
      <text class="label">{{item.key}}</text>
      <text class="value">{{item.value}}</text>
    </view>
  </view>
  <view wx:if="{{product.description}}" class="section">
    <view class="section-title">商品详情</view>
    <rich-text nodes="{{product.description}}" />
  </view>
  <view class="footer">展示商品 · 到店咨询 · 不支持线上下单</view>
</view>
<view wx:else class="empty">{{loading ? '加载中…' : '商品不存在'}}</view>
